[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aws-quest"
version = "0.0.1"

[tool.hatch.build.targets.wheel]
packages = ["src/aws_quest"]

[project.scripts]
aws-quest = "aws_quest.app:main"

